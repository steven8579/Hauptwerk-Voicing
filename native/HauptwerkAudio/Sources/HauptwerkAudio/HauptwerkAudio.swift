// Public C-compatible entry points for the HauptwerkAudio framework.
// Consumed via P/Invoke from HauptwerkVoicing.Audio.Mac.
//
// Phase 1: capture side (enumerate, open, start, stop, close, calibration tone).
// Phase 2: adds output-side functions for synchronous sweep playback + capture.

import Foundation
